
name: CI

on:
  push:
    branches: [ "test" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/anyakichi/aosp-builder:14.04
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.github_token }}


    steps:
      - uses: actions/checkout@v3
        with: 
          repository: 'MiCode/Xiaomi_Kernel_OpenSource'
          ref: latte-l-oss
          fetch-depth: '1'

      - name: Clone  tools
        run: |
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/x86/x86_64-linux-android-4.9
      - name: (Debug)Output build env
        run: |
          ls
          ls x86_64-linux-android-4.9

      - name: Build
        run: |
          export ARCH=x86_64
          export SUBARCH=x86_64
          export CROSS_COMPILE=x86_64-linux-android-4.9/bin/x86_64-linux-android-

          mkdir out

          make O=out defconfig
          make O=out -j8
